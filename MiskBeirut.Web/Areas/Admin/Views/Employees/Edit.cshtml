@model EmployeeFormViewModel
@using MiskBeirut.Web.Areas.Admin.Models.Employees
@{
    ViewData["Title"] = "Edit Employee";
}

<div class="max-w-lg mx-auto bg-surface-container-lowest border border-surface-container-high rounded-DEFAULT p-container-padding">
    <h2 class="font-headline-lg text-headline-lg text-on-surface mb-stack-lg">Edit Employee</h2>
    <div asp-validation-summary="All" class="mb-stack-md font-body-md text-body-md text-brick-red"></div>
    <form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="space-y-stack-md">
        <input type="hidden" asp-for="Id" />
        <div>
            <label class="block font-label-sm text-label-sm text-on-surface-variant uppercase mb-stack-xs">Name</label>
            <input asp-for="Name" class="w-full rounded-DEFAULT border border-border-subtle px-4 py-2 focus:border-slate-blue focus:ring-slate-blue" />
        </div>
        <div>
            <label class="block font-label-sm text-label-sm text-on-surface-variant uppercase mb-stack-xs">Position</label>
            <input asp-for="Position" class="w-full rounded-DEFAULT border border-border-subtle px-4 py-2 focus:border-slate-blue focus:ring-slate-blue" />
        </div>
        <div>
            <label class="block font-label-sm text-label-sm text-on-surface-variant uppercase mb-stack-xs">Phone Number</label>
            <input asp-for="PhoneNumber" class="w-full rounded-DEFAULT border border-border-subtle px-4 py-2 focus:border-slate-blue focus:ring-slate-blue" />
        </div>
        <div>
            <label class="block font-label-sm text-label-sm text-on-surface-variant uppercase mb-stack-xs">Base Salary</label>
            <input asp-for="BaseSalary" type="number" step="0.01" class="w-full rounded-DEFAULT border border-border-subtle px-4 py-2 focus:border-slate-blue focus:ring-slate-blue" />
        </div>
        <label class="flex items-center gap-2">
            <input asp-for="IsActive" type="checkbox" class="rounded border-border-subtle" />
            <span class="font-body-md text-body-md text-on-surface-variant">Active</span>
        </label>
        <button type="submit" class="w-full py-3 bg-primary hover:bg-primary-container text-white font-label-lg text-label-lg rounded-DEFAULT transition-colors">Save Changes</button>
    </form>
</div>
