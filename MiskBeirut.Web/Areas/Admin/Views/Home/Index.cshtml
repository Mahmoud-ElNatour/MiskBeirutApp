@{
    ViewData["Title"] = "Dashboard";
}

<div class="bg-white dark:bg-slate-900 border border-primary/10 rounded-xl p-8 shadow-sm">
    <h2 class="text-2xl font-bold text-slate-900 dark:text-white">Welcome back, @User.Identity?.Name</h2>
    <p class="text-slate-500 mt-2">The Admin console screens (Daily Closing, Employees, Customers, Expenses, Receivers, Audit Logs, Users) will be built out here in the next phase.</p>
</div>
