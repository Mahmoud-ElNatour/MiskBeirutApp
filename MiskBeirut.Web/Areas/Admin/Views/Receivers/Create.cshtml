@model ReceiverFormViewModel
@using MiskBeirut.Web.Areas.Admin.Models.Receivers
@{
    ViewData["Title"] = "New Receiver";
}

<div class="max-w-lg mx-auto bg-surface-container-lowest border border-surface-container-high rounded-DEFAULT p-container-padding">
    <h2 class="font-headline-lg text-headline-lg text-on-surface mb-stack-lg">New Receiver</h2>
    <div asp-validation-summary="All" class="mb-stack-md font-body-md text-body-md text-brick-red"></div>
    <form asp-action="Create" method="post" class="space-y-stack-md">
        <div>
            <label class="block font-label-sm text-label-sm text-on-surface-variant uppercase mb-stack-xs">Name</label>
            <input asp-for="Name" class="w-full rounded-DEFAULT border border-border-subtle px-4 py-2 focus:border-slate-blue focus:ring-slate-blue" />
        </div>
        <button type="submit" class="w-full py-3 bg-primary hover:bg-primary-container text-white font-label-lg text-label-lg rounded-DEFAULT transition-colors">Create Receiver</button>
    </form>
</div>
