@model IEnumerable<MiskBeirut.Core.Entities.Credits>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Credit Records";
    var currentMonth = ViewData["CurrentMonth"] as int? ?? DateTime.UtcNow.Month;
    var currentYear = ViewData["CurrentYear"] as int? ?? DateTime.UtcNow.Year;
    var total = ViewData["Total"] as decimal? ?? 0;
    var count = ViewData["Count"] as int? ?? 0;
    var max = ViewData["Max"] as decimal? ?? 0;
}

<!-- Header -->
<div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 mb-8">
    <div>
        <h1 class="text-3xl font-bold text-slate-900 dark:text-white">Credit Records</h1>
        <p class="text-slate-500 dark:text-slate-400 mt-1">View and filter customer credit sales.</p>
    </div>
    <div class="flex flex-wrap items-center gap-3 w-full md:w-auto pb-2">
        <a href="/System/ControlPanel"
            class="inline-flex justify-center items-center gap-2 px-4 py-2 bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-700 rounded-lg text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-700/50 transition-colors">
            <span class="material-symbols-outlined text-sm">arrow_back</span>
            Back
        </a>
        <select id="recordMonth"
            class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-800 text-sm focus:ring-primary focus:border-primary text-slate-700 dark:text-slate-300">
            <option value="">All Months</option>
            @for (int m = 1; m <= 12; m++)
            {
                <option value="@m" selected="@(m == currentMonth ? "selected" : null)">
                    @(new DateTime(2000, m, 1).ToString("MMMM"))
                </option>
            }
        </select>
        <select id="recordYear"
            class="rounded-lg border-slate-300 dark:border-slate-700 dark:bg-slate-800 text-sm focus:ring-primary focus:border-primary text-slate-700 dark:text-slate-300">
            <option value="">All Years</option>
            @for (int y = 2023; y <= 2030; y++)
            {
                <option value="@y" selected="@(y == currentYear ? "selected" : null)">@y</option>
            }
        </select>
        <button onclick="filterRecords()"
            class="inline-flex justify-center items-center gap-2 px-4 py-2 bg-white dark:bg-slate-800 border border-slate-300 dark:border-slate-700 rounded-lg text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-700/50 transition-colors">
            <span class="material-symbols-outlined text-sm">filter_list</span>
            Filter
        </button>
    </div>
</div>

<!-- Statistics Summary -->
<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
    <div class="bg-primary text-white rounded-xl shadow-sm p-6 relative overflow-hidden group">
        <div class="absolute right-0 top-0 opacity-10 transform translate-x-1/4 -translate-y-1/4 group-hover:scale-110 transition-transform duration-500">
            <span class="material-symbols-outlined text-8xl">payments</span>
        </div>
        <div class="relative z-10">
            <h6 class="text-sm font-semibold text-primary-100 uppercase tracking-wider mb-2">Total Credit Sales</h6>
            <h3 class="text-3xl font-bold">$@total.ToString("F2")</h3>
        </div>
    </div>

    <div class="bg-slate-800 dark:bg-slate-700 text-white rounded-xl shadow-sm p-6 relative overflow-hidden group">
        <div class="absolute right-0 top-0 opacity-10 transform translate-x-1/4 -translate-y-1/4 group-hover:scale-110 transition-transform duration-500">
            <span class="material-symbols-outlined text-8xl">tag</span>
        </div>
        <div class="relative z-10">
            <h6 class="text-sm font-semibold text-slate-300 uppercase tracking-wider mb-2">Record Count</h6>
            <h3 class="text-3xl font-bold">@count</h3>
        </div>
    </div>

    <div class="bg-indigo-500 dark:bg-indigo-600 text-white rounded-xl shadow-sm p-6 relative overflow-hidden group">
        <div class="absolute right-0 top-0 opacity-10 transform translate-x-1/4 -translate-y-1/4 group-hover:scale-110 transition-transform duration-500">
            <span class="material-symbols-outlined text-8xl">trending_up</span>
        </div>
        <div class="relative z-10">
            <h6 class="text-sm font-semibold text-indigo-100 uppercase tracking-wider mb-2">Max Single Credit</h6>
            <h3 class="text-3xl font-bold">$@max.ToString("F2")</h3>
        </div>
    </div>
</div>

<!-- Records Table -->
<div class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-xl shadow-sm overflow-hidden mb-6">
    <div class="overflow-x-auto">
        <table class="w-full text-left text-sm whitespace-nowrap">
            <thead class="bg-slate-50 dark:bg-slate-800/50 border-b border-slate-200 dark:border-slate-800 text-slate-500 dark:text-slate-400">
                <tr>
                    <th class="px-6 py-4 font-semibold" data-sort="number">ID</th>
                    <th class="px-6 py-4 font-semibold" data-sort="date">Date</th>
                    <th class="px-6 py-4 font-semibold" data-sort="text">Customer</th>
                    <th class="px-6 py-4 font-semibold text-right" data-sort="number">Amount</th>
                    <th class="px-6 py-4 font-semibold flex-1">Note</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-slate-200 dark:divide-slate-800">
                @if (Model != null && Model.Any())
                {
                    foreach (var record in Model)
                    {
                        <tr class="hover:bg-slate-50 dark:hover:bg-slate-800/50 transition-colors">
                            <td class="px-6 py-4 text-slate-500 dark:text-slate-400">@record.Id</td>
                            <td class="px-6 py-4 font-medium text-slate-900 dark:text-white">@record.Date.ToString("yyyy-MM-dd")</td>
                            <td class="px-6 py-4 text-slate-600 dark:text-slate-300">
                                <span class="inline-flex items-center px-2 py-1 rounded-md bg-slate-100 dark:bg-slate-800 text-xs font-medium text-slate-600 dark:text-slate-300">
                                    @(record.Customer?.Username ?? "Unknown")
                                </span>
                            </td>
                            <td class="px-6 py-4 text-right font-medium text-red-600 dark:text-red-400">-$@record.Amount.ToString("F2")</td>
                            <td class="px-6 py-4 text-slate-600 dark:text-slate-300">@(record.Note ?? "N/A")</td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="px-6 py-12 text-center text-slate-500 dark:text-slate-400">
                            <div class="flex flex-col items-center justify-center">
                                <span class="material-symbols-outlined text-4xl mb-3 text-slate-300 dark:text-slate-600">payments</span>
                                <p class="text-base font-medium">No credit records found</p>
                                <p class="text-sm mt-1">Adjust your filters to see data here.</p>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
<script>
    function filterRecords() {
        const month = document.getElementById('recordMonth').value;
        const year = document.getElementById('recordYear').value;
        const params = new URLSearchParams();
        if (month) params.append('month', month);
        if (year) params.append('year', year);
        window.location.href = `/control-panel/credits?${params.toString()}`;
    }

    document.addEventListener('DOMContentLoaded', function () {
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.has('month')) document.getElementById('recordMonth').value = urlParams.get('month');
        if (urlParams.has('year')) document.getElementById('recordYear').value = urlParams.get('year');
    });
</script>
}
